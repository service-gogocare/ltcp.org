rules_version = '2';

// 這個資料庫已經沒有用途。
//
// 2026-09-09：程式改成「資料放在各機構自己的 Google 試算表」，我方不再持有任何
// 使用者資料，firebase 套件也從程式裡整個移除了。舊有的 organizations／users／
// audit_logs 三個集合已用 firestore:delete --all-collections 清空。
//
// 專案本身**不能刪** —— Google 登入的 OAuth 用戶端與 Picker 的 API 金鑰都掛在
// 這個專案（編號 3023017484）底下，刪了線上的程式當場就登不進去。
//
// 所以留下一個空資料庫，並且一律拒絕存取。這同時解決了一件懸很久的事：
// 先前兩個修補提權漏洞的規則變更（57e362b、3e3450a）從來沒有 deploy 過，
// 線上跑的一直是有漏洞的舊版。既然資料已經清空且不再使用，
// 正確的規則就是「什麼都不准」—— 那比修補那個漏洞更徹底。
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}

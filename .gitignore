# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Env config
.env

# Data files
*.xlsx
*.xls
*.csv

# Agent skills (本機開發輔助工具，含 __pycache__，不進版控)
.agents/

# 小卡維運腳本的輸出（含身分證號與姓名等個資，不進版控）
card-backup-*.json

# 放在專案根目錄的參考文件（規格書、操作手冊、商標原圖、法務條文原稿…）。
# 只忽略根目錄那一層 —— public/ 與 src/ 底下是程式真正要用的資產，照常進版控。
# 之前 `git add -A` 就把一份 7.6MB 的操作手冊 PDF 掃進 commit 過。
/*.pdf
/*.png
/*.jpg
/*.jpeg
/*.txt
/*.docx

# 資料存放在各機構自己的 Google 雲端硬碟（一機構一份試算表），
# 這個程式不持有任何使用者資料，也沒有伺服器與後端資料庫。
#
# 註：曾經有 VITE_BACKEND 這個變數用來切換 Firestore／試算表兩種儲存層，
# 2026-09-09 連同 firebase 套件整個移除 —— 現在只有一種實作，設它也沒有作用。

# Google Cloud Console 建立的「OAuth 2.0 用戶端 ID（網頁應用程式）」，
# 授權的 JavaScript 來源要包含正式網域與 http://localhost:5173。
# 純前端流程不需要 client secret。
VITE_GOOGLE_CLIENT_ID=your_oauth_client_id_here.apps.googleusercontent.com

# Google Cloud Console 建立的「API 金鑰」，供 Google Picker 使用
# （與上面的用戶端 ID 是兩回事，兩者都需要）。
# 這把金鑰會被內嵌進公開的 JS（瀏覽器端 OAuth 本來就是這樣），
# **所以請在金鑰設定裡限制「HTTP 參照網址」為你的網域與 http://localhost:5173/*，
# 並限制只能使用 Google Picker API** —— 參照網址限制是唯一的防護。
# 沒有這把金鑰時，「機構名冊沒出現？」會明確提示缺少設定，其餘功能不受影響。
VITE_GOOGLE_API_KEY=your_api_key_here
